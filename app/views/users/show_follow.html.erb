<div class="posts_container">
  <div class="sprout">
    <h2 class="posts_front"><i class="fas fa-carrot"></i>フォロー中</h2>
  </div>
  <% @user.followings.each do |user| %>
    <table>
      <tr>
        <td><%= user.username %></td>
        <td><%= link_to '詳細', user_path(user) %></td>
      </tr>
    </table>
  <% end %>
</div>